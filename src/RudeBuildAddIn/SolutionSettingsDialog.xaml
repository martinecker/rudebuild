<Window x:Class="RudeBuildAddIn.SolutionSettingsDialog"
             Name="_window"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:RudeBuild="clr-namespace:RudeBuild;assembly=RudeBuild"
             xmlns:RudeBuildAddIn="clr-namespace:RudeBuildAddIn"
             Title="RudeBuild Solution Settings" Width="500" Height="533">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="36*" />
            <RowDefinition Height="28" />
            <RowDefinition Height="36" />
        </Grid.RowDefinitions>
        <Label Height="25" Margin="6,0,6,0" Grid.Row="0" 
               Content="Files Excluded from RudeBuild per Project:" />
        <TreeView Name="_treeViewExcludedFileNames" Margin="6,0,6,0" Grid.Row="1" ToolTip="Right click project to add, right click file to remove."
                  ItemsSource="{Binding Path=ProjectNameToExcludedCppFileNameMap}" SelectedValuePath="Key" PreviewMouseRightButtonDown="OnPreviewMouseRightButtonDown" 
                  TreeViewItem.Selected="OnTreeViewItemSelected" TreeViewItem.Unselected="OnTreeViewItemUnselected">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Value}">
                    <TextBlock FontWeight="Bold" Text="{Binding Path=Key}">
                        <TextBlock.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="_Add Excluded C/C++ File..." Click="OnAddExcludedCppFileNameForProject" />
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                    </TextBlock>
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}">
                                <TextBlock.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="_Remove Excluded C/C++ File" Click="OnRemoveExcludedCppFileNameForProject" />
                                    </ContextMenu>
                                </TextBlock.ContextMenu>
                            </TextBlock>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="true" />
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
        <CheckBox Content="Disable Precompiled Headers" Margin="8,6,6,0" Grid.Row="2" ToolTip="If checked, precompiled headers will be turned off for all projects in the solution even if normally enabled."
                  IsChecked="{Binding Path=DisablePrecompiledHeaders}" />
        <StackPanel Orientation="Horizontal" Grid.Row="3">
            <Button Content="OK" IsDefault="True" Height="25" HorizontalAlignment="Right" Margin="315,0,6,0" Width="75" Click="OnOK" />
            <Button Content="Cancel" IsCancel="True"  Height="25" HorizontalAlignment="Right" Margin="0,0,6,0" Width="75" Click="OnCancel" />
        </StackPanel>
    </Grid>
</Window>
